---
output: html_document
editor_options: 
  chunk_output_type: console
format:
  html:
    html-table-processing: none
---

# Inventories {.unnumbered}

```{r packages and functions}
#| include: false
library(tidyverse)
library(gt)
library(yaml)
source("script/utils.R")
options(readr.show_col_types = FALSE)

params <- read_yaml("data/config.yml")

print_inventory <- function(ref, pattern, cols, return_tibble = FALSE) {
  tbl_inventory <- 
    read_rds("data_generated/works.rds") %>%
      left_join(
        read_csv("data/catalogue_overview.csv") %>% distinct(group, file),
        by = "group"
      ) %>%
      unite(group:number, col = "id", sep = ".", na.rm = TRUE) %>%
      mutate(id = str_glue("[{id}](/groups/{file}.html#work-{id})")) %>%
      separate_longer_delim(inventories, ", @") %>%
      filter(str_detect(inventories, ref)) %>%
      rowwise() %>%
      mutate(
        entries =
          str_match_all(inventories, pattern)[[1]] %>%
          magrittr::set_colnames(c("match", cols)) %>%
          as_tibble() %>%
          select(!match) %>%
          list()
      ) %>%
      unnest(entries) %>%
      select(all_of(cols), id) %>% 
      mutate(across(!id, parse_guess)) %>%
      arrange(pick(!id))
  
  if (return_tibble)
    return(tbl_inventory)
  
  tbl_inventory %>% 
    summarise(.by = all_of(cols), id = str_flatten_comma(id)) %>% 
    arrange(across(all_of(cols), \(c) str_rank(c, numeric = TRUE))) %>% 
    gt() %>%
    fmt_markdown(columns = id) %>%
    cols_label(id = params$catalogue_prefix) %>% 
    tab_options(
      column_labels.font.weight = "bold",
      table.align = "left",
      table_body.hlines.style = "none"
    )
}
```



## Historic

### @BrtniceInv

```{r Brtnice}
#| echo: false
#| output: asis
print_inventory(
  "BrtniceInv",
  pattern = "p. (\\w+), no. (\\w+)",
  cols = c("p.", "no.")
)
```


### @EisenstadtInv

The inventory of Eisenstadt was finalized in 1837. The table of sources is written on the verso pages, which are numbered (with odd numbers!). For selected entries, the recto pages contain annotations indicating new parts that should be created (column “notes” in the table below). Groups have been numbered with uppercase letters. Thereby, the combination of group letter and inventory number corresponds to the modern shelfmark used by A-Ed. The column “parts” (originally “Partes” and “Stück”) denotes the number of parts that existed when the inventory was assembled.

```{r Eisenstadt}
#| echo: false

inv <-
  print_inventory(
    "EisenstadtInv",
    pattern = "p. (\\w+), no. (\\w+)",
    cols = c("p.", "no."),
    return_tibble = TRUE
  ) %>%
  summarise(
    .by = c(p., no.),
    id = str_flatten_comma(id)
  )

works_eisenstadt <-
  read_csv("data/inventory_eisenstadt.csv") %>% 
  fill(group, page)

# all citations of inventory in work table are in inventory table and vice versa
stopifnot(
  anti_join(
    inv,
    works_eisenstadt,
    by = join_by(p. == page, no. == number)
  ) %>%
    nrow == 0L
)

stopifnot(
  anti_join(
    works_eisenstadt,
    inv,
    by = join_by(page == p., number == no.)
  ) %>%
    nrow == 0L
)

works_eisenstadt %>%
  left_join(
    inv,
    by = join_by(page == p., number == no.)
  ) %>% 
  select(!shelfmark) %>%
  gt(groupname_col = "group") %>%
  cols_align("right", columns = c(page, number, parts)) %>%
  cols_label(page = "p.", number = "no.", id = params$catalogue_prefix) %>% 
  fmt_markdown(columns = id) %>%
  sub_missing(columns = notes, missing_text = "") %>%
  tab_options(
    column_labels.font.weight = "bold",
    row_group.background.color = "grey90",
    table_body.hlines.style = "none"
  )
```


### @EsterhazyInv1740

```{r Esterhazy 1740}
#| echo: false
#| output: asis

inv <- print_inventory(
  "EsterhazyInv1740",
  pattern = "sec. (\\w+), no. (\\w+)",
  cols = c("section", "no."),
  return_tibble = TRUE
)

works_esterhazy <-
  read_csv("data/inventory_esterhazy1740.csv") %>% 
  fill(group) %>% 
  separate_wider_regex(
    group,
    patterns = c(section = "\\w", ".*"),
    cols_remove = FALSE
  )

# all citations of inventory in work table are in inventory table
stopifnot(
  anti_join(
    inv,
    works_esterhazy,
    by = join_by(no. == number, section == section)
  ) %>%
    nrow == 0L
)

works_esterhazy %>%
  left_join(
    inv,
    by = join_by(number == no., section == section)
  ) %>% 
  select(!section) %>%
  summarise(
    .by = c(group, number, title),
    id = str_flatten_comma(id)
  ) %>%
  gt(groupname_col = "group") %>%
  cols_align("right", columns = number) %>%
  cols_label(number = "no.", id = params$catalogue_prefix) %>%
  fmt_markdown(columns = id) %>%
  sub_missing(columns = id, missing_text = "") %>%
  tab_options(
    column_labels.font.weight = "bold",
    row_group.background.color = "grey90",
    table_body.hlines.style = "none"
  )
```


### @EsterhazyInv1805

```{r Esterhazy 1805}
#| echo: false
#| output: asis
print_inventory(
  "EsterhazyInv1805",
  pattern = "fol. (\\w+), no. (\\w+)",
  cols = c("fol.", "no.")
)
```


### @EsterhazyInv1812

```{r Esterhazy 1812}
#| echo: false
#| output: asis
print_inventory(
  "EsterhazyInv1812",
  pattern = "([\\w\\s]+) no. ([\\w-]+)",
  cols = c("genre", "no.")
)
```


### @HerzogenburgInvB

```{r Herzogenburg B}
#| echo: false
#| output: asis
print_inventory(
  "HerzogenburgInvB",
  pattern = "fol. (\\w+), no. ([\\w-]+)",
  cols = c("fol.", "no.")
)
```


### @Sauer1809

```{r Sauer}
#| echo: false
#| output: asis
print_inventory(
  "Sauer1809",
  pattern = "fol. (\\w+), no. ([\\w-]+)",
  cols = c("fol.", "no.")
)
```


### @Werner1737

```{r Werner1737}
#| echo: false
#| output: asis
inv <- print_inventory(
  "Werner1737",
  pattern = "p. (\\w+), no. (\\w+)\\.(\\w+)(\\??)",
  cols = c("p.", "section", "no.", "putative"),
  return_tibble = TRUE
)

works_werner <-
  read_csv("data/inventory_werner1737.csv") %>% 
  fill(group, page) %>% 
  separate_wider_regex(
    group,
    patterns = c("\\[", section = "\\w+", "\\].*"),
    cols_remove = FALSE
  )

# all citations of inventory in work table are in inventory table
stopifnot(
  anti_join(
    inv,
    works_werner,
    by = join_by(p. == page, no. == number, section == section)
  ) %>%
    nrow == 0L
)

works_werner %>%
  left_join(
    inv,
    by = join_by(page == p., number == no., section == section)
  ) %>% 
  select(!section) %>%
  unite(id, putative, col = "id", sep = "", na.rm = TRUE) %>% 
  summarise(
    .by = c(group, page, number, title), 
    id = str_flatten_comma(id)
  ) %>% 
  gt(groupname_col = "group") %>%
  cols_align("right", columns = c(page, number)) %>%
  cols_label(page = "p.", number = "no.", id = params$catalogue_prefix) %>%
  fmt_markdown(columns = id) %>%
  sub_missing(columns = id, missing_text = "") %>%
  tab_options(
    column_labels.font.weight = "bold",
    row_group.background.color = "grey90",
    table_body.hlines.style = "none"
  )
```



## Modern

### @Bardos1976

```{r Bardos 1976}
#| echo: false
#| output: asis
print_inventory(
  "Bardos1976",
  pattern = "p. (\\w+), no. (\\w+)",
  cols = c("p.", "no.")
)
```


### @Bardos1980

```{r Bardos 1980}
#| echo: false
#| output: asis
print_inventory(
  "Bardos1980",
  pattern = "p. (\\w+), no. ([\\w/\\(\\)]+)",
  cols = c("p.", "no.")
)
```


### @Bardos1984

```{r Bardos 1984}
#| echo: false
#| output: asis
print_inventory(
  "Bardos1984",
  pattern = "p. (\\w+), no. ([\\w/]+)",
  cols = c("p.", "no.")
)
```


### @Riedel1979

```{r Riedel}
#| echo: false
#| output: asis
print_inventory(
  "Riedel1979",
  pattern = "p. (\\w+), no. (\\w+)",
  cols = c("p.", "no.")
)
```


### @Rostetter2023

```{r Rostetter}
#| echo: false
#| output: asis
print_inventory(
  "Rostetter2023",
  pattern = "p. (\\w+), no. ([\\w/]+)",
  cols = c("p.", "no.")
)
```


### @Stefan1985

```{r Stefan}
#| echo: false
#| output: asis
print_inventory(
  "Stefan1985",
  pattern = "p. (\\w+), no. (\\w+)",
  cols = c("p.", "no.")
)
```


## Fonds Moder

[Richard Moder](https://data.onb.ac.at/nlv_lex/perslex/M/Moder_Richard.html) (1901–1991) studied Gregor Joseph Werner's music intensively and published some of his works for the first time. His Nachlass concerning Werner is available in the Austrian National Library and includes around 190 inventory items. (Another partial Nachlass is archived by the University of Salzburg; however, it does not contain any items related to Werner.) For each item, the following table lists

- its inventory number, linked to the library's catalogue entry
- the folder (some items are collected in a common folder)
- the title
- the catalogue of works number
- the classification
- Moder's numbering present on the source

Items in gray text have not yet been screened.



```{r fonds moder}
#| echo: false

# read contents of the fonds Moder
fonds_data <- 
  read_csv("data/fonds_moder.csv") %>% 
  rename(WerW = id)

# check that entries in the fonds Moder have correct WerW numbers
fonds_cited <- 
  read_rds("data_generated/works.rds") %>% 
  unnest(sources, names_sep = "_") %>%
  filter(str_starts(sources_source, "A-Wn F100")) %>%
  unite(group:number, col = "WerW", sep = ".", na.rm = TRUE) %>%
  select(number = sources_source, WerW) %>%
  mutate(number = str_replace(number, "A-Wn F100.Moder.", ""))

wrong_sources <- 
  fonds_data %>% 
  filter(!is.na(WerW)) %>%
  anti_join(fonds_cited, by = join_by(number, WerW)) %>% 
  filter(check_link)

if (nrow(wrong_sources) > 0) {
  error("wrong WerW number in Fonds Moder ",
        "{str_flatten_comma(wrong_sources$number)}",
        strict = TRUE)
}

# define links to the works
work_links <- 
  read_rds("data_generated/works.rds") %>% 
  left_join(
    read_csv("data/catalogue_overview.csv") %>% distinct(group, file),
    by = "group"
  ) %>% 
  unite(group:number, col = "WerW", sep = ".", na.rm = TRUE) %>%
  mutate(
    link = str_glue('<a href="/groups/{file}.html#work-{WerW}">{WerW}</a>')
  ) %>%
  select(WerW, link) %>% 
  add_row(WerW = "–", link = "–")

# create the table output
fonds_data %>% 
  left_join(work_links, by = "WerW") %>% 
  mutate(
    number =
      if_else(
        is.na(onb),
        number,
        str_glue('<a href="https://data.onb.ac.at/rec/{onb}">{number}</a>')
      ) %>%
      map(html),
    WerW = 
      link %>% 
      replace_na("") %>% 
      map(html)
  ) %>%
  select(number, folder, title, WerW, classification, number_moder, notes) %>%
  gt() %>%
  cols_align("left") %>%
  cols_label(number_moder = "number (Moder)") %>% 
  fmt_markdown(columns = notes) %>%
  sub_missing(columns = c(folder, number_moder, notes), missing_text = "") %>%
  tab_style(
    style = cell_text(color = "grey70"),
    locations = cells_body(rows = is.na(folder))
  ) %>%
  tab_options(
    column_labels.font.weight = "bold",
    table_body.hlines.style = "none"
  )
```
